% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-dr.R
\name{save_dr_plot}
\alias{save_dr_plot}
\title{Save a reduction plot, widened to fit its own legend.}
\usage{
save_dr_plot(
  plot,
  file_prefix,
  file_format = c("png", "pdf"),
  width = NULL,
  height = 6,
  panel_width = height
)
}
\arguments{
\item{plot}{A ggplot object, such as from \code{\link[=plot_dr_group]{plot_dr_group()}}. Forwarded
straight to \code{ggsave(plot = )}.}

\item{file_prefix}{Path to save to without an extension.}

\item{file_format}{File formats to write.}

\item{width}{(optional) Saved width in inches. \code{NULL} measures \code{plot}'s
legend and adds it to \code{panel_width}.}

\item{height}{Saved height in inches.}

\item{panel_width}{Width of the plot panel itself, excluding the legend, in
inches. Only used when \code{width} is \code{NULL}.}
}
\value{
\code{plot}, invisibly.
}
\description{
Unlike \code{\link[=dr_plot_width]{dr_plot_width()}} (a label-count guess made before the plot exists),
this measures the legend actually rendered on \code{plot} - so long label text
widens the saved image even when the label count is small. Requires an
open graphics device to get accurate text metrics, so it opens and closes
a throwaway one around the measurement.
}
