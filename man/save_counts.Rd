% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{save_counts}
\alias{save_counts}
\title{Save the counts matrix as a single table.}
\usage{
save_counts(x, assay = "RNA", layer = "data", digits = 3, file)
}
\arguments{
\item{x}{A Seurat object.}

\item{assay}{Assay to retrieve the counts from.}

\item{layer}{Layer to retrieve. The default "data" layer holds the normalized
and log-transformed expression used for visualization and most
differential expression tests.}

\item{digits}{Number of digits to round the values to. Ignored for a layer
that already holds whole numbers, such as raw counts.}

\item{file}{Path to write the table to as a csv, or \code{NULL} to skip writing
and just return the tibble. Has no default: every caller states where the
table goes.}
}
\value{
A tibble with one row per gene.
}
\description{
Layers left split by a \code{merge()} are rejoined first, since \code{LayerData()}
would otherwise return only the first of them, and warn rather than error
while doing it.
}
