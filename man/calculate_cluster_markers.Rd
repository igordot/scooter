% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{calculate_cluster_markers}
\alias{calculate_cluster_markers}
\title{Calculate cluster markers (versus all other clusters, or pairwise) and plot
them.}
\usage{
calculate_cluster_markers(x, label, test, pairwise = FALSE)
}
\arguments{
\item{x}{Seurat object with an identity set.}

\item{label}{Suffix for the output filenames.}

\item{test}{Statistical test - see above.}

\item{pairwise}{Compare clusters pairwise instead of each cluster against all
others - see above.}
}
\value{
\code{x}, invisibly. Called for its file and plot output.
}
\description{
\code{test}, forwarded to \code{\link[Seurat:FindAllMarkers]{Seurat::FindAllMarkers()}}/\code{\link[Seurat:FindMarkers]{Seurat::FindMarkers()}}:
\code{"wilcox"} (Wilcoxon rank sum, the default), \code{"roc"} (classification power
from 0 = random to 1 = perfect), \code{"bimod"} (McDavid et al. 2013), \code{"tobit"}
(Trapnell et al. 2014), or \code{"MAST"} (Finak et al. 2015).
}
\details{
\code{pairwise = TRUE} compares each cluster against each other cluster
individually (rather than each cluster against all others combined), keeping
only genes significant in every comparison - local and global markers both,
at the cost of being much slower for many clusters.
}
