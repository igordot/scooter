% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-dr.R
\name{plot_dr_group}
\alias{plot_dr_group}
\title{Scatter plot of a reduction, colored by a grouping variable.}
\usage{
plot_dr_group(
  x,
  reduction = "umap",
  group_by = NULL,
  color_scheme = NULL,
  pt_size = NULL,
  na_value = "grey50",
  file_prefix = NULL,
  file_format = c("png", "pdf"),
  width = NULL,
  height = 6
)
}
\arguments{
\item{x}{Seurat object.}

\item{reduction}{Dimensionality reduction to plot on (e.g. "umap", "tsne",
"pca").}

\item{group_by}{(optional) Metadata column to color by. \code{NULL} uses the
current identities.}

\item{color_scheme}{(optional) Vector of colors. Defaults to the "clusters"
scheme.}

\item{pt_size}{(optional) Point size. Defaults to \code{get_dr_point_size(x)}.}

\item{na_value}{Color for cells with no value in \code{group_by}.}

\item{file_prefix}{(optional) Path to save to.}

\item{file_format}{File formats to write when \code{file_prefix} is given.}

\item{width}{(optional) Saved width in inches. \code{NULL} measures the plot's
own legend and widens to fit it - see \code{\link[=save_dr_plot]{save_dr_plot()}}.}

\item{height}{Saved height in inches.}
}
\value{
A ggplot object.
}
\description{
The categorical counterpart of \code{\link[=plot_dr_feature]{plot_dr_feature()}}: cells are colored by a
metadata column (or by the current identities) rather than by a continuous
feature. Cells are shuffled so no one group is drawn on top of the others.
}
