% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{filter_cells}
\alias{filter_cells}
\title{Filter cells based on the number of genes, counts, and mitochondrial reads.}
\usage{
filter_cells(
  x,
  num_mads = 3,
  min_genes = 500,
  max_genes = NULL,
  min_counts = NULL,
  max_counts = NULL,
  max_mt = 10,
  min_cells = 50,
  log_file = NULL
)
}
\arguments{
\item{x}{A tibble with metadata (default method) or a Seurat object (Seurat
method). Must have \code{detected_genes}/\code{total_counts} columns, as added by
\code{\link[=initialize_seurat_object]{initialize_seurat_object()}}.}

\item{num_mads}{Median absolute deviations for the outlier stage. \code{NULL}
skips the stage.}

\item{min_genes}{Minimum number of genes per cell. \code{NULL} for no cutoff.}

\item{max_genes}{Maximum number of genes per cell. \code{NULL} for no cutoff.}

\item{min_counts}{Minimum number of counts per cell. \code{NULL} for no cutoff.}

\item{max_counts}{Maximum number of counts per cell. \code{NULL} for no cutoff.}

\item{max_mt}{Maximum percentage of mitochondrial reads per cell. \code{NULL} for
no cutoff.}

\item{min_cells}{Minimum number of cells that must survive filtering. Below
this, filtering \code{stop()}s rather than handing back an object too small to
analyze meaningfully. \code{NULL} skips the check.}

\item{log_file}{Log file.}
}
\value{
Filtered data. The Seurat method also writes a pre-filtering QC
violin plot (via \code{\link[=plot_metrics_distribution]{plot_metrics_distribution()}}) and the unfiltered
metadata (via \code{\link[=save_metadata]{save_metadata()}}, as \code{metadata-unfiltered.csv.gz}), plus
the post-filtering QC plots (via \code{\link[=plot_metrics_distribution]{plot_metrics_distribution()}} and
\code{\link[=plot_metrics_correlations]{plot_metrics_correlations()}}) to the working directory.
}
\description{
Filtering runs in two stages, and the log records how many cells each one
removed.
}
\details{
\enumerate{
\item \strong{Outlier removal.} Cells further than \code{num_mads} median absolute
deviations from the median number of genes or counts are dropped. The
median and MAD are computed on the log scale and the bounds
back-transformed, which suits these right-skewed counts. \code{num_mads = NULL} skips it.
\item \strong{Fixed cutoffs.} \code{min_genes} / \code{max_genes} / \code{min_counts} /
\code{max_counts} are applied to whatever survived stage 1, then \code{max_mt}.
Each is skipped when \code{NULL}, so out of the box only \code{min_genes} (500) and
\code{max_mt} (10) do anything.
}

Applying the fixed cutoffs second makes them a quality floor the data-driven
bounds cannot undercut: a wide or contaminated distribution can put the MAD
bound below 500 genes, and those cells are still removed. All cutoffs are
inclusive.

Genes that are no longer detected in any of the retained cells are dropped,
since filtering cells leaves them as all-zero rows.
}
