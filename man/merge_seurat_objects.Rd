% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{merge_seurat_objects}
\alias{merge_seurat_objects}
\title{Merge multiple Seurat objects into one.}
\usage{
merge_seurat_objects(seurat_object_list, min_cells = NULL, log_file = NULL)
}
\arguments{
\item{seurat_object_list}{List of Seurat objects and/or analysis directory
paths (mixing both is fine).}

\item{min_cells}{Keep genes detected in at least this many cells. If \code{NULL},
10 is used, or 0.1\% of the cells when there are more than 50,000 of them.}

\item{log_file}{Filename for the log file.}
}
\value{
A merged Seurat object.
}
\description{
Merges a list of Seurat objects, rejoins the split layers that Seurat 5
leaves behind, and removes genes that are detected in too few cells.
Features of the non-RNA assays (such as ADT and HTO) are always kept.
}
\details{
Elements of \code{seurat_object_list} may be Seurat objects, or paths to an
analysis directory containing a \code{seurat_obj.qs2} (as written by the
CLI). Which one it is does not matter to the caller: this function
resolves each element to an object first.

Once every input is an object, this function strips each one's own
reductions, scaled data, and clustering columns before merging. A
per-sample PCA, tSNE/UMAP, or clustering — written by
\code{\link[=create_seurat_object]{create_seurat_object()}}/\code{\link[=cluster_seurat_object]{cluster_seurat_object()}} — is meaningless once
merged. \code{\link[=normalize_counts]{normalize_counts()}}/\code{\link[=run_pca]{run_pca()}} recompute all of it downstream
anyway.

Writes \code{metrics-distribution.png}, a per-sample QC violin plot, to the
working directory.
}
