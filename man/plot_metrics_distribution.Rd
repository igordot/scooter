% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-qc.R
\name{plot_metrics_distribution}
\alias{plot_metrics_distribution}
\title{Violin plots of the per-cell QC metrics.}
\usage{
plot_metrics_distribution(
  x,
  metrics,
  group_by = "orig.ident",
  color_scheme = NULL,
  file = NULL,
  width = 12,
  height = 6
)
}
\arguments{
\item{x}{Seurat object.}

\item{metrics}{Metadata columns to plot, one panel each. No default - the
caller decides, since the right set depends on context (e.g. a single
sample can afford \code{pct_ribo}/\code{pct_hb} alongside
\code{detected_genes}/\code{total_counts}/\code{pct_mito}; a multi-sample plot usually
can't). A \verb{pct_*} metric that is under 1\% in every cell is skipped (a flat
near-zero violin says nothing) - its range is reported with a message
instead.}

\item{group_by}{Metadata column to group the violins by.}

\item{color_scheme}{(optional) Named vector of colors. Defaults to the
"samples" scheme, named by the sorted levels of \code{group_by}.}

\item{file}{Path to save the plot to. \code{NULL} returns it without writing.
Parent directories are created as needed.}

\item{width, height}{Saved size in inches. Widen it when there are many
samples.}
}
\value{
A combined plot, one panel per plotted metric.
}
\description{
One violin panel per metric, grouped by sample. The metrics are metadata
columns, so \code{layer} is set explicitly: \code{VlnPlot()} otherwise searches for
the "data" layer, which does not exist before \code{NormalizeData()} has been
run.
}
