% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate.R
\name{variable_features_by_batch}
\alias{variable_features_by_batch}
\title{Variable features of each batch of a layer-split object.}
\usage{
variable_features_by_batch(x, assay = "RNA")
}
\arguments{
\item{x}{Seurat object with split layers.}

\item{assay}{Assay to read the variable features from.}
}
\value{
A named list of variable gene vectors, one element per batch.
}
\description{
After \code{\link[=split_layers_by_batch]{split_layers_by_batch()}} the per-batch variable features are stored
as \verb{vf_<method>_<layer>_variable} columns in the assay meta data rather than
being reachable through \code{VariableFeatures()}, which returns the combined
set.
}
\details{
\code{\link[=normalize_counts]{normalize_counts()}} now clears its own assay's \verb{vf_*} columns before
recomputing them. But an object built before that guard existed can still
carry a whole history of them: each sample's own pre-merge run from
\code{\link[=create_seurat_object]{create_seurat_object()}}, and the merge step's own post-merge preview
run. \code{DietSeurat()} clears layers, reductions, and graphs, but never
assay meta data. \code{merge()} disambiguates colliding names with a numeric
suffix, for example \code{vf_vst_counts.1_variable}.

Matching every \verb{_variable$} column would pick up that whole history along
with the batches actually being asked about. This function instead
anchors on \code{Layers(x[[assay]], search = "counts")}, the object's current
split state. That keeps stale columns out, regardless of which
generation of the object this is.
}
