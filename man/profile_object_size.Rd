% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{profile_object_size}
\alias{profile_object_size}
\title{Report the size of an object's slots, largest first.}
\usage{
profile_object_size(x, max_depth = 3)
}
\arguments{
\item{x}{Any object - a Seurat object, a piece pulled from one (an assay via
\code{x[["RNA"]]}, a reduction via \code{x[["pca"]]}, ...), a plain list, or
anything else \code{object.size()} accepts.}

\item{max_depth}{How many levels to descend before reporting a subtree's
total size instead of continuing to break it down.}
}
\value{
A tibble with one row per slot/element, largest first: \code{path} (its
position, \code{" > "}-separated) and \code{size} (human-readable, e.g. "1.2 Mb").
}
\description{
Works on any S4 object or list, not just Seurat objects. It descends into
S4 slots and list elements, bounded by \code{max_depth}. So a Seurat object,
an assay pulled from one (\code{x[["RNA"]]}), a \code{DimReduc}, or a plain nested
list all get the same recursive breakdown.
}
\details{
An \code{Assay5}'s \code{layers} slot is itself a list. So
\code{layers > counts}/\code{layers > data}/\code{layers > scale.data} are sized
separately, not lumped into one \code{layers} total.

A matrix, data frame, or atomic vector is neither S4 nor a list. Passing
one of these just reports its own size, the same as calling
\code{object.size()} directly. There is nothing to descend into.
}
