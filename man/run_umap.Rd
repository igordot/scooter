% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr.R
\name{run_umap}
\alias{run_umap}
\alias{run_umap.default}
\alias{run_umap.Seurat}
\title{Run UMAP}
\usage{
run_umap(x, ...)

\method{run_umap}{default}(x, num_neighbors = 30, min_dist = 0.3, suffix = "", ...)

\method{run_umap}{Seurat}(
  x,
  num_neighbors = 30,
  min_dist = 0.3,
  suffix = "",
  assay = "RNA",
  reduction = NULL,
  num_dim = NULL,
  features = NULL,
  var_features = FALSE,
  graph = NULL,
  file_format = c("png", "pdf"),
  ...
)
}
\arguments{
\item{x}{A matrix (default) or a Seurat object.}

\item{...}{Passed to the underlying function.}

\item{num_neighbors}{Number of neighbors.}

\item{min_dist}{Minimum distance between points in the embedding.}

\item{suffix}{Tag inserted into the reduction/column names.}

\item{assay}{Assay to use (Seurat method). Always set explicitly.}

\item{reduction}{Existing reduction to take dimensions from (Seurat method).}

\item{num_dim}{Number of dimensions to take from \code{reduction} (Seurat method).}

\item{features}{Explicit features to use (Seurat method).}

\item{var_features}{Use the assay's variable features (Seurat method).}

\item{graph}{Graph to use as input (Seurat method).}

\item{file_format}{File formats to write the scatter plot in (Seurat method).}
}
\value{
A UMAP embedding matrix (default) or a Seurat object with the
reduction added. The Seurat method also writes a sample-colored scatter of
the result to the working directory, as \verb{dr.<reduction>.png}/\code{.pdf} (or
just \code{.png} if \code{file_format = "png"}).
}
\description{
The default method runs \code{uwot::umap2()} on a matrix; the Seurat method runs
\code{Seurat::RunUMAP()} with \code{umap.method = "uwot2"} (also \code{uwot::umap2()}) and
stores a \code{"umap<suffix>"} reduction. Defaults match \code{Seurat::RunUMAP()} (30
neighbors, minimum distance 0.3) rather than the lower \code{uwot} defaults.
}
