% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{initialize_seurat_object}
\alias{initialize_seurat_object}
\title{Create a new Seurat object from a matrix.}
\usage{
initialize_seurat_object(
  counts_matrix,
  assay = "RNA",
  min_cells = 1,
  min_genes = 1,
  project = "proj",
  log_file = NULL
)
}
\arguments{
\item{counts_matrix}{A matrix of raw counts.}

\item{assay}{Seurat assay to add the data to.}

\item{min_cells}{Include genes/features detected in at least this many cells.}

\item{min_genes}{Include cells where at least this many genes/features are
detected.}

\item{project}{Project name for Seurat object.}

\item{log_file}{Filename for the logfile.}
}
\value{
Seurat object.
}
\description{
For an RNA assay the per-cell QC metrics are added at the same time:
\code{pct_mito}/\code{pct_ribo}/ \code{pct_hb} (via \code{\link[=add_gene_class_percent]{add_gene_class_percent()}}), plus
\code{detected_genes} and \code{total_counts} as clearer aliases of
\code{nFeature_RNA}/\code{nCount_RNA}, and \code{sample_name} as a clearer alias of
\code{orig.ident}. All six are metadata columns as soon as the object exists,
so \code{\link[=plot_metrics_distribution]{plot_metrics_distribution()}} (which takes its \code{metrics} explicitly, no
default) and \code{\link[=plot_metrics_correlations]{plot_metrics_correlations()}} can use any of them without
further preparation.
}
