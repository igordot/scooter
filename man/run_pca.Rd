% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr.R
\name{run_pca}
\alias{run_pca}
\alias{run_pca.default}
\alias{run_pca.Seurat}
\title{Run PCA}
\usage{
run_pca(x, ...)

\method{run_pca}{default}(x, num_pcs = 50, suffix = "", ...)

\method{run_pca}{Seurat}(
  x,
  num_pcs = 50,
  suffix = "",
  assay = "RNA",
  features = NULL,
  var_features = FALSE,
  ...
)
}
\arguments{
\item{x}{A matrix (default) or a Seurat object.}

\item{...}{Passed to the underlying function.}

\item{num_pcs}{Number of principal components.}

\item{suffix}{Tag inserted into the reduction/column names.}

\item{assay}{Assay to use (Seurat method). Always set explicitly.}

\item{features}{Explicit features to use (Seurat method).}

\item{var_features}{Use the assay's variable features (Seurat method).}
}
\value{
A named list (default) or a Seurat object with the reduction added
(Seurat method). The Seurat method also writes the diagnostic plots to the
working directory: a sample-colored scatter and an elbow plot, plus a
heatmap past 15 components.
}
\description{
The default method runs \code{irlba::prcomp_irlba()} on a matrix; the Seurat
method runs \code{Seurat::RunPCA()} on the object and stores a \code{"pca<suffix>"}
reduction.
}
