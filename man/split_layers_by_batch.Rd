% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split.R
\name{split_layers_by_batch}
\alias{split_layers_by_batch}
\title{Split a Seurat object into per-batch layers and prepare it for integration.}
\usage{
split_layers_by_batch(
  x,
  batch_var = "orig.ident",
  assay = "RNA",
  num_variable_genes = 3000,
  num_dim = 50,
  log_file = NULL
)
}
\arguments{
\item{x}{Seurat object.}

\item{batch_var}{Metadata column to split the layers by.}

\item{assay}{Assay to split.}

\item{num_variable_genes}{Number of variable features. Matches the
single-sample default.}

\item{num_dim}{Number of principal components to compute.}

\item{log_file}{Filename for the log file.}
}
\value{
A Seurat object with split layers and a "pca" reduction.
}
\description{
Drops the scaled data, reductions, graphs, and clustering columns, then
splits the assay layers by a metadata column — splitting the layers rather
than the object is what the Seurat 5 \code{IntegrateLayers()} workflow expects.
}
\details{
Everything after the split runs through the same verbs as the single-sample
path (\code{\link[=normalize_counts]{normalize_counts()}}, \code{\link[=run_pca]{run_pca()}}), so the two workflows cannot drift
apart on parameters. \code{\link[=integrate_layers]{integrate_layers()}} then consumes the resulting "pca"
reduction.
}
