% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate.R
\name{integrate_layers}
\alias{integrate_layers}
\title{Integrate the layers of a Seurat object.}
\usage{
integrate_layers(
  x,
  assay = "RNA",
  int_reduction = "cca",
  num_dim,
  k_anchor = 10,
  k_weight = 100,
  log_file = NULL,
  ...
)
}
\arguments{
\item{x}{Seurat object with split layers and a "pca" reduction.}

\item{assay}{Assay to integrate.}

\item{int_reduction}{Integration method: "cca", "rpca", or "harmony".}

\item{num_dim}{Number of dimensions to use (5-50). For "cca"/"rpca", this
value is passed as \code{dims = 1:num_dim} to
\code{CCAIntegration()}/\code{RPCAIntegration()}, which both take a real \code{dims}
argument. \code{HarmonyIntegration()} has no \code{dims} parameter. It runs on the
full \code{"pca"} embedding as-is, through \code{Embeddings(orig)}, unsliced. So
for \code{int_reduction = "harmony"}, \code{num_dim} only takes effect indirectly,
through however many components \code{orig.reduction} ("pca") already has.
The normal \code{\link[=integrate_seurat_object]{integrate_seurat_object()}} pipeline is unaffected:
\code{\link[=split_layers_by_batch]{split_layers_by_batch()}} already computes that "pca" reduction with
exactly \code{num_dim} components. A direct call is different. If \code{x}'s
"pca" reduction already has more components than \code{num_dim}, then
\code{num_dim} has no effect on the harmony path.}

\item{k_anchor}{Neighbors to use when picking anchors. Ignored by "harmony".
A value larger than Seurat's default of 5 strengthens the alignment.}

\item{k_weight}{Neighbors to use when weighting the corrections. Must be
smaller than the number of cells in the smallest batch. Reduced to 25 when
the smallest batch has fewer than 100 cells. Batches smaller than 25 cells
are an error.}

\item{log_file}{Filename for the log file.}

\item{...}{Extra arguments for whichever method \code{IntegrateLayers()}
dispatches to - e.g. \code{theta}/\code{lambda}/\code{sigma}/\code{tau}/\code{nclust} for
"harmony", or \code{k.filter}/\code{sample.tree}/ \code{normalization.method} for
"cca"/"rpca". An unrecognized name errors (via \code{\link[rlang:check_dots_used]{rlang::check_dots_used()}})
rather than being silently dropped by the method function's own \code{...}.}
}
\value{
A Seurat object with rejoined layers and an \code{int_reduction}-named
reduction (\code{"cca"}, \code{"rpca"}, or \code{"harmony"}).
}
\description{
Runs the Seurat 5 \code{IntegrateLayers()} workflow. It adds what calling
\code{IntegrateLayers()} directly leaves for the caller to handle:
}
\details{
\itemize{
\item Clamps \code{k_weight} to fit the smallest batch. Seurat's own default of 100
errors outright below that size. See \code{k_weight} below.
\item Excludes \code{dims}/\code{k.anchor} for \code{"harmony"}. \code{HarmonyIntegration()}
silently ignores both instead of erroring, so passing them would rely
on that silent drop. See \code{num_dim}/\code{k_anchor} below.
\item Forces \code{future::plan(future::sequential)}. Benchmarks show multisession
gives no gain here, and is a documented liability at this scale
elsewhere in this package.
\item Rejoins the split layers with \code{JoinLayers()} afterward.
\code{IntegrateLayers()} itself leaves this undone.
}

Unlike the older anchor workflow, this function creates no "integrated"
assay. Clustering and visualization use the corrected embedding directly,
instead of re-running PCA. This function expects
\code{\link[=split_layers_by_batch]{split_layers_by_batch()}} to have run first. It is the one step of
\code{\link[=integrate_seurat_object]{integrate_seurat_object()}} that calls \code{IntegrateLayers()} directly.
}
