% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{normalize_counts}
\alias{normalize_counts}
\title{Normalize the counts, select variable features, and scale.}
\usage{
normalize_counts(
  x,
  method = "log",
  num_variable_genes = 3000,
  assay = "RNA",
  log_file = NULL
)
}
\arguments{
\item{x}{A Seurat object.}

\item{method}{"log" for \code{NormalizeData()} + \code{FindVariableFeatures()} +
\code{ScaleData()} (log-normalized to a scale factor of 10,000), or "sct" for
\code{SCTransform()}.}

\item{num_variable_genes}{Number of variable features.}

\item{assay}{Assay to normalize.}

\item{log_file}{Log file.}
}
\value{
The processed Seurat object. Also writes the variable-feature plot
(\code{variance-features.png}) to the working directory.
}
\description{
A thin wrapper that picks the right Seurat function for \code{method} and fixes
the parameters this pipeline uses, so either path hands back a fully
processed object — no separate variance step needed. \code{method = "sct"}
already worked this way natively: \code{SCTransform()} produces the counts,
variable features and scaled data in one pass. \code{method = "log"} now mirrors
that explicitly: \code{NormalizeData()} followed by \code{FindVariableFeatures()} and
\code{ScaleData()}.
}
\details{
No covariates are regressed out (\code{vars.to.regress = NULL}) for either path.
Germain et al. (2020) found that the "common practice of regressing out cell
covariates, such as the detection rate or proportion of mitochondrial
reads[,] nearly always had a negative impact."

On the log path, this function drops any \verb{vf_*} columns already on
\code{assay}'s feature meta data before recomputing them. An earlier call can
leave these columns behind: each sample's own pre-merge run, or a merge
step's preview run. Left alone, they would just accumulate call after
call. See \code{\link[=variable_features_by_batch]{variable_features_by_batch()}} for what reading an accumulated
column list, instead of a current one, leads to.

\code{SCTransform()} never writes this kind of per-gene bookkeeping column. So
the sct path has nothing to clean up. The check runs the same way for
both paths; it is a no-op for sct, not something conditioned on \code{method}.
}
