% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-qc.R
\name{plot_metrics_correlations}
\alias{plot_metrics_correlations}
\title{Scatter plots of the per-cell QC metrics against each other.}
\usage{
plot_metrics_correlations(
  x,
  pairs = list(c("total_counts", "detected_genes"), c("pct_mito", "detected_genes"),
    c("pct_mito", "pct_ribo")),
  group_by = "orig.ident",
  color_scheme = NULL,
  file = NULL,
  width = 16,
  height = 5
)
}
\arguments{
\item{x}{Seurat object.}

\item{pairs}{A list of length-2 character vectors, \code{c(feature1, feature2)},
one per panel.}

\item{group_by}{Metadata column to color the points by.}

\item{color_scheme}{(optional) Named vector of colors. Defaults to the
"samples" scheme, named by the sorted levels of \code{group_by}.}

\item{file}{Path to save the plot to. \code{NULL} returns it without writing.
Parent directories are created as needed.}

\item{width, height}{Saved size in inches. The default is wide and short: one
square panel per pair of metrics, side by side.}
}
\value{
A combined plot, one panel per pair.
}
\description{
One panel per pair in \code{pairs}, colored by sample. No legend, since this only
ever runs on a single sample - see \code{\link[=filter_cells]{filter_cells()}}. Pairs are a curated
list rather than every combination of a metric vector, since that would make
the panel count quadratic in the number of metrics (5 metrics -> 10 panels
via \code{combn()}) and most pairs are not biologically meaningful together.
}
