% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_clusters}
\alias{plot_clusters}
\title{Plot a cluster resolution on tSNE and UMAP.}
\usage{
plot_clusters(
  x,
  resolution,
  filename_base,
  color_scheme = get_color_scheme("clusters")
)
}
\arguments{
\item{x}{Seurat object.}

\item{resolution}{Metadata column (or bare resolution value, resolved via
\code{\link[=check_identity_column]{check_identity_column()}}) to set as the identity before plotting.}

\item{filename_base}{Path prefix for the two plots (\code{-tsne}/\code{-umap} are
appended by \code{\link[=plot_dr_group]{plot_dr_group()}}).}

\item{color_scheme}{(optional) Named vector of cluster colors. Defaults to
\code{get_color_scheme("clusters")}.}
}
\value{
\code{x} with \code{resolution} set as its identity.
}
\description{
A single cluster says nothing, and past the color scheme's length the colors
start repeating, so nothing is plotted outside \code{2:length(color_scheme)}
clusters.
}
