% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{create_seurat_object}
\alias{create_seurat_object}
\title{Create a usable Seurat object from a sample.}
\usage{
create_seurat_object(
  path,
  sample_name,
  num_mads = 3,
  min_genes = 500,
  max_genes = NULL,
  min_counts = NULL,
  max_counts = NULL,
  max_mt = 10,
  normalization_method = "log",
  num_variable_genes = 3000,
  num_pcs = 50,
  num_dim = 30,
  log_file = NULL
)
}
\arguments{
\item{path}{Path to a 10x directory or a flat counts file.}

\item{sample_name}{Sample/library name, used as the cell ID prefix.}

\item{num_mads, min_genes, max_genes, min_counts, max_counts, max_mt}{Quality
cutoffs, passed to \code{\link[=filter_cells]{filter_cells()}} — see there for how the outlier stage
and the fixed cutoffs combine.}

\item{normalization_method}{Normalization method, passed to
\code{\link[=normalize_counts]{normalize_counts()}} (as its own \code{method} argument): "log" or "sct".}

\item{num_variable_genes}{Number of variable features.}

\item{num_pcs}{Principal components to compute. Reduced automatically when
the object is too small to support that many.}

\item{num_dim}{Number of dimensions for the first-pass tSNE/UMAP, capped at
\code{num_pcs}.}

\item{log_file}{Filename for the log file.}
}
\value{
A Seurat object with normalized data, variable features, scaled
data, and "pca", "tsne", and "umap" reductions — ready for
\code{\link[=cluster_seurat_object]{cluster_seurat_object()}}. Also writes the variable-feature, PCA, tSNE,
and UMAP diagnostic plots (via \code{\link[=normalize_counts]{normalize_counts()}}, \code{\link[=run_pca]{run_pca()}},
\code{\link[=run_tsne]{run_tsne()}}, and \code{\link[=run_umap]{run_umap()}}) and the metadata+embeddings table (via
\code{\link[=save_metadata]{save_metadata()}}, as \code{metadata.csv.gz}) to the working directory.
}
\description{
Everything needed to take a sample from a counts file to an object ready
for clustering, in one call.
}
\details{
The function runs these steps:
\enumerate{
\item Read the counts (\code{\link[=read_counts_file]{read_counts_file()}}).
\item Build the object (\code{\link[=initialize_seurat_object]{initialize_seurat_object()}}).
\item Attach any antibody capture data as an "ADT" assay
(\code{\link[=add_seurat_assay]{add_seurat_assay()}}).
\item Apply the quality cutoffs (\code{\link[=filter_cells]{filter_cells()}}).
\item Normalize, select variable features, and scale
(\code{\link[=normalize_counts]{normalize_counts()}}, which does all three for either method).
\item Run PCA (\code{\link[=run_pca]{run_pca()}}).
\item Compute a first-pass tSNE/UMAP (\code{\link[=run_tsne]{run_tsne()}}, \code{\link[=run_umap]{run_umap()}}).
}

This function attaches the ADT assay \emph{before} filtering, on purpose.
\code{\link[=add_seurat_assay]{add_seurat_assay()}} restricts the object to the barcodes both matrices
share. Attaching ADT after filtering would apply the cutoffs to a
different set of cells.

PCA, and the tSNE/UMAP computed from it, runs on whichever assay
normalization left active. \code{SCTransform()} creates and activates "SCT".
The log path leaves "RNA" active. So \code{dr_assay} is decided directly from
\code{normalization_method}, not read back off the object. Reading it back off
\code{DefaultAssay()} would reintroduce the implicit dependency an explicit
\code{assay} argument exists to avoid.

The tSNE/UMAP computed here are a first-pass look, capped at \code{num_dim}
dimensions. \code{\link[=cluster_seurat_object]{cluster_seurat_object()}} recomputes both once a clustering
resolution's own \code{num_dim} is chosen. So this pair is not the final one
used for the actual clustering plots.
}
