% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{transfer_labels}
\alias{transfer_labels}
\title{Transfer labels from a reference Seurat object}
\usage{
transfer_labels(
  query,
  ref,
  query_label_col,
  ref_label_col,
  query_assay,
  ref_assay,
  normalization,
  ref_reduction = "pca",
  num_dim,
  anchor_reduction = "pcaproject",
  ref_umap = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{query}{Query Seurat object.}

\item{ref}{Reference Seurat object - already integrated and annotated.}

\item{query_label_col}{Metadata column name added to \code{query} for the
transferred labels.}

\item{ref_label_col}{Metadata column in \code{ref} holding the labels to
transfer (cell type, or any other category).}

\item{query_assay}{Assay in \code{query} to use for anchor finding. \code{"RNA"}
(log-normalized) or \code{"SCT"}, depending on how \code{query} was built. No
default, like the params below: this and \code{ref_assay}/\code{normalization}/
\code{num_dim} all describe how a specific object was built.}

\item{ref_assay}{Assay in \code{ref} to use for anchor finding.}

\item{normalization}{Normalization method shared by \code{ref} and \code{query}
(\code{"log"}/\code{"LogNormalize"} or \code{"sct"}/\code{"SCT"}).
\code{\link[Seurat:FindTransferAnchors]{Seurat::FindTransferAnchors()}} takes one value for both objects, so
\code{ref} and \code{query} must actually share a method.}

\item{ref_reduction}{Reduction in \code{ref} to project \code{query} onto.}

\item{num_dim}{Number of dimensions to use for anchor finding and label
transfer.}

\item{anchor_reduction}{Dimensional reduction workflow
\code{\link[Seurat:FindTransferAnchors]{Seurat::FindTransferAnchors()}} uses to find anchors.}

\item{ref_umap}{If \code{TRUE}, also runs \code{\link[Seurat:MapQuery]{Seurat::MapQuery()}} and adds a
\code{"ref.umap"} reduction to \code{query}, projecting it onto \code{ref}'s UMAP. \code{ref}
needs a UMAP built with \code{return.model = TRUE}, which this function does
not check for - \code{MapQuery()} errors on its own if it is missing.}

\item{verbose}{Print progress messages.}
}
\value{
\code{query} with \code{query_label_col} (a factor) added to its metadata,
and a \code{"ref.umap"} reduction added if \code{ref_umap = TRUE}. Also writes
the predictions table (\code{cell}/\code{predicted.id}/\code{prediction.score.max}) to
\verb{annotation/annotation-transfer-label-<query_label_col>.csv.gz}, and a
\verb{dr-<reduction>-<query_label_col>.png} UMAP colored by the transferred
labels for each of \code{"umap"}/\code{"ref.umap"} already present on \code{query}.
}
\description{
Reference-based annotation wrapping Seurat's reference-mapping workflow
(\code{\link[Seurat:FindTransferAnchors]{Seurat::FindTransferAnchors()}} + \code{\link[Seurat:TransferData]{Seurat::TransferData()}}) to annotate
\code{query} with labels (cell type, or any other categorical metadata) from an
already integrated and annotated reference object. Optionally also projects
\code{query} onto the reference's UMAP via \code{\link[Seurat:MapQuery]{Seurat::MapQuery()}}.
}
