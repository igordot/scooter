% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate.R
\name{integrate_seurat_object}
\alias{integrate_seurat_object}
\title{Integrate a merged Seurat object across batches.}
\usage{
integrate_seurat_object(
  x,
  assay = "RNA",
  int_reduction = "cca",
  num_dim,
  batch_var = "orig.ident",
  num_variable_genes = 3000,
  num_neighbors = 30,
  k_anchor = 10,
  k_weight = 100,
  log_file = NULL,
  ...
)
}
\arguments{
\item{x}{Merged Seurat object, such as the output of
\code{\link[=merge_seurat_objects]{merge_seurat_objects()}}.}

\item{assay}{Assay to integrate. Passed straight through to
\code{\link[=integrate_layers]{integrate_layers()}}.}

\item{int_reduction}{Integration method: "cca", "rpca", or "harmony".}

\item{num_dim}{Number of dimensions to use (5-50).}

\item{batch_var}{Metadata column identifying the batch.}

\item{num_variable_genes}{Number of variable features per batch.}

\item{num_neighbors}{Neighbors for UMAP.}

\item{k_anchor}{Neighbors to use when picking anchors. Passed straight
through to \code{\link[=integrate_layers]{integrate_layers()}} - see there for defaults/caveats (ignored
by "harmony").}

\item{k_weight}{Neighbors to use when weighting the corrections. Passed
straight through to \code{\link[=integrate_layers]{integrate_layers()}} - see there for defaults/caveats
(also ignored by "harmony").}

\item{log_file}{Filename for the log file.}

\item{...}{Passed straight through to \code{\link[=integrate_layers]{integrate_layers()}} - extra
method-specific tuning arguments (harmony's \code{theta}/\code{lambda}/\code{sigma}/...,
or cca/rpca's \code{k.filter}/\code{sample.tree}/...).}
}
\value{
A Seurat object with rejoined layers, an \code{int_reduction}-named
reduction (\code{"cca"}, \code{"rpca"}, or \code{"harmony"}), and "tsne"/"umap"
reductions computed from it. Also writes the variable-gene overlap, QC
distribution, integrated-embedding, and tSNE/UMAP plots to the working
directory.
}
\description{
The integrate step of the pipeline in one call. It plays the same role for
a merged object that \code{\link[=create_seurat_object]{create_seurat_object()}} plays for a single sample.
}
\details{
The function runs these steps:
\enumerate{
\item Split the assay layers by batch and prepare them
(\code{\link[=split_layers_by_batch]{split_layers_by_batch()}}).
\item Report how much the per-batch variable genes overlap
(\code{\link[=plot_var_genes_euler]{plot_var_genes_euler()}}, \code{\link[=plot_var_genes_upset]{plot_var_genes_upset()}}).
\item Correct the batch effect (\code{\link[=integrate_layers]{integrate_layers()}}).
\item Compute a first-pass tSNE/UMAP on the corrected embedding.
}

The tSNE/UMAP from step 4 is a preview, capped at \code{num_dim}. It differs
from the real one \code{\link[=cluster_seurat_object]{cluster_seurat_object()}} computes later over its own
\code{num_dim}. \code{\link[=cluster_seurat_object]{cluster_seurat_object()}} has no default \code{reduction}. Pass it
this function's \code{int_reduction} name explicitly. Otherwise it may use the
uncorrected "pca" reduction that \code{\link[=split_layers_by_batch]{split_layers_by_batch()}} also leaves on
the object.
}
