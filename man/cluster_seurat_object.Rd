% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_seurat_object}
\alias{cluster_seurat_object}
\title{Reduce dimensions, cluster, and plot the result.}
\usage{
cluster_seurat_object(
  x,
  assay = "RNA",
  reduction,
  num_dim,
  num_neighbors = 30,
  metadata_file = NULL,
  log_file = NULL
)
}
\arguments{
\item{x}{Seurat object with \code{reduction} computed, such as the output of
\code{\link[=create_seurat_object]{create_seurat_object()}} (which only ever produces "pca") or
\code{\link[=integrate_seurat_object]{integrate_seurat_object()}} (which also produces one named after its
integration method: "cca", "rpca", or "harmony").}

\item{assay}{Assay to attribute the tSNE/UMAP reductions to.
\code{\link[=create_seurat_object]{create_seurat_object()}} creates its own assay, so it needs no such
parameter. This function creates none: it only calls
\code{\link[=run_tsne]{run_tsne()}}/\code{\link[=run_umap]{run_umap()}}, which read from an existing assay. So
\code{assay} is explicit here, rather than read back off \code{DefaultAssay(x)}.}

\item{reduction}{Reduction to compute tSNE/UMAP from and to cluster on -
"pca" after \code{\link[=create_seurat_object]{create_seurat_object()}}, or the integration method's own name
("cca"/"rpca"/"harmony") after \code{\link[=integrate_seurat_object]{integrate_seurat_object()}} to cluster on
the batch-corrected embedding instead of the pre-correction one. No
default: which reduction is the intended one for clustering differs by
which pipeline branch got you here, so every caller states it explicitly
rather than risk silently picking the wrong one.}

\item{num_dim}{Principal components to use, bounded to 5-50.}

\item{num_neighbors}{Neighbors for UMAP and for the SNN graph.}

\item{metadata_file}{Path to write the cell metadata and embeddings to, via
\code{\link[=save_metadata]{save_metadata()}}. \code{NULL} writes nothing.}

\item{log_file}{Filename for the log file.}
}
\value{
A Seurat object with "tsne" and "umap" reductions and one \verb{res.<x>}
column per retained clustering resolution.
}
\description{
The clustering step of the pipeline in one call: run tSNE and UMAP on an
existing "pca" reduction (\code{\link[=run_tsne]{run_tsne()}}, \code{\link[=run_umap]{run_umap()}}), build the SNN graph
and cluster over a range of resolutions (\code{\link[=calculate_clusters]{calculate_clusters()}}), and plot
each reduction by cluster.
}
\details{
This function computes both reductions before clustering, since the
cluster plots draw on them. \code{\link[=run_tsne]{run_tsne()}} and \code{\link[=run_umap]{run_umap()}} already write
their own sample-colored scatter to the working directory, so this
function skips plotting "by sample" again.

The per-resolution cluster plots also write unconditionally, into a
\verb{clusters-resolutions/} subdirectory. This matches every other verb's
standard-output convention.
}
