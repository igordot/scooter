% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-dr.R
\name{plot_dr_feature}
\alias{plot_dr_feature}
\title{Plot a single feature overlaid on a dimensionality reduction.}
\usage{
plot_dr_feature(
  x,
  feature,
  assay = NULL,
  reduction = "umap",
  cells = NULL,
  color_scheme = NULL,
  pt_size = NULL
)
}
\arguments{
\item{x}{Seurat object.}

\item{feature}{Feature name (gene, antibody, or metadata column) to plot.}

\item{assay}{(optional) Assay to fetch \code{feature} from. \code{NULL} uses
\code{DefaultAssay(x)}.}

\item{reduction}{Dimensionality reduction to plot on (e.g. "umap", "tsne",
"pca").}

\item{cells}{(optional) Cells to plot, in the given order (controls z-order
of overlapping points). Defaults to a random shuffle of all cells so no
single cell/group dominates due to plot order.}

\item{color_scheme}{(optional) Vector of colors for the low-to-high
expression gradient.}

\item{pt_size}{(optional) Point size. Defaults to \code{get_dr_point_size(x)}.}
}
\value{
A ggplot object.
}
\description{
One point per cell, colored by expression/abundance of \code{feature} (a gene,
antibody, or any other value \code{FeaturePlot()} can fetch). Unlike passing a
multi-color ramp straight to \code{FeaturePlot(cols = )}, the ramp is applied
afterward with \code{scale_color_gradientn()} so the color scale stays
continuous - see the "cols" note below.
}
\section{Seurat's \code{FeaturePlot(cols = )} quirk}{

As of Seurat 5, passing a \code{cols} vector of anything other than exactly 2
colors makes \code{FeaturePlot()} bin the continuous values into 2 groups before
coloring (\code{cut(x, breaks = 2)}), which turns a smooth expression gradient
into a flat low/high split. Seurat 4 only did this when \code{cols} had exactly 2
colors, so a long custom ramp used to pass through untouched. This function
sidesteps the quirk (and the version difference) entirely by never passing a
multi-color \code{cols} to \code{FeaturePlot()} - the ramp is layered on afterward
instead.
}

