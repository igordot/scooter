# Get geneset scores.

Get geneset scores.

## Usage

``` r
geneset_score(module_tbl, counts_raw, min_cpm = 0, limit_pct = 1)
```

## Arguments

- module_tbl:

  geneset table.

- counts_raw:

  Raw counts

- min_cpm:

  .

- limit_pct:

  .

## Value

.
