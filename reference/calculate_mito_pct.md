# Calculate mitochondrial percentage from Seurat object.

Deprecated: renamed to
[`add_gene_class_percent()`](https://igordot.github.io/scooter/reference/add_gene_class_percent.md),
which also adds `pct_ribo` and `pct_hb`.

## Usage

``` r
calculate_mito_pct(x)
```

## Arguments

- x:

  A Seurat object.

## Value

Seurat object.
